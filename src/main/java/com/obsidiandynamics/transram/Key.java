package com.obsidiandynamics.transram;

public interface Key {
//  @SuppressWarnings({"unchecked", "rawtypes"})
//  static int compare(Key a, Key b) {
//    final var isInternalA = a instanceof InternalKey;
//    final var isInternalB = b instanceof InternalKey;
//    if (isInternalA && isInternalB) {
//      final var internalA = (InternalKey) a;
//      final var internalB = (InternalKey) b;
//      return internalA.compareTo(internalB);
//    } else if (isInternalA) {
//      return 1;
//    } else if (isInternalB) {
//      return -1;
//    } else {
//      final var wrapperA = (WrapperKey) a;
//      final var wrapperB = (WrapperKey) b;
//      return wrapperA.unwrap().compareTo(wrapperB.unwrap());
//    }
//  }
}
